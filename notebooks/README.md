# notebooks/

Exploration only. Anything that produces a reported figure or number must be
promoted to a script under `scripts/` with a config under `configs/experiments/`.

Suggested starting points:
- `00_synthetic_walkthrough.ipynb` — run the README quick start interactively.
- `01_calibration_inspection.ipynb` — visualise stain vectors, flat-fields and paired-slide residuals.
- `02_criteria_on_paired_slides.ipynb` — per-criterion invariance plots.
