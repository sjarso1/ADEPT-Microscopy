# Architecture Decision Records

Every decision that changes what a reported number means gets an ADR:
architecture arm selection (S2-ID-1), canonical stain matrix replacement,
criterion threshold changes, schema version bumps, scope decisions (S3-ID-1
binary vs three-way), split re-creation.

File name: `NNNN-short-title.md`. Copy `0000-template.md`.
